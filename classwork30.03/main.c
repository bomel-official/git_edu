#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <stdbool.h>

#define ARR_SIZE 10

int main() {
    setlocale(LC_ALL, "Rus");

    
    // int i = 0;

    // int* ptrArr = (int*)malloc(ARR_SIZE * sizeof(int));
    // for (; i < ARR_SIZE; i++) {
    //     ptrArr[0] = i + 15;
    // }

    

    return 0;
}